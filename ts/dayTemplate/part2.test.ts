import { assertEquals } from "https://deno.land/std@0.167.0/testing/asserts.ts";
import { solve } from "./part2.ts";

const input = ``;

Deno.test("solve", () => {
  assertEquals(solve(input), input);
});
